[project]
name = "cover-art"
version = "0.1.0"
requires-python = ">=3.11"

[tool.uv]
package = false

[tool.uv.workspace]
members = ["packages/render", "packages/album-covers", "packages/album-store"]
